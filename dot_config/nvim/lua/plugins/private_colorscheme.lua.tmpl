{{- $theme := .theme.name -}}
{{- $themeData := include (printf "themes/%s.yaml" $theme) | fromYaml -}}
-- Theme: {{ $theme }}
-- Auto-generated by chezmoi - DO NOT EDIT MANUALLY

return {
{{- if hasPrefix "gruvbox" $theme }}
  -- Gruvbox theme
  {
    "{{ $themeData.tools.nvim_plugin }}",
    priority = 1000,
    opts = {
      terminal_colors = true,
      undercurl = true,
      underline = true,
      bold = true,
      italic = {
        strings = true,
        emphasis = true,
        comments = true,
        operators = false,
        folds = true,
      },
      strikethrough = true,
      invert_selection = false,
      invert_signs = false,
      invert_tabline = false,
      invert_intend_guides = false,
      inverse = true,
      contrast = "{{ $themeData.tools.nvim_opts.contrast }}",
      palette_overrides = {},
      overrides = {},
      dim_inactive = false,
      transparent_mode = {{ $themeData.tools.nvim_opts.transparent_mode }},
    },
  },
{{- else if hasPrefix "catppuccin" $theme }}
  -- Catppuccin theme
  {
    "{{ $themeData.tools.nvim_plugin }}",
    name = "catppuccin",
    priority = 1000,
    opts = {
      flavour = "{{ $themeData.tools.nvim_opts.flavour }}",
      background = {
        light = "latte",
        dark = "{{ $themeData.tools.nvim_opts.flavour }}",
      },
      transparent_background = {{ $themeData.tools.nvim_opts.transparent_background }},
      term_colors = true,
      styles = {
        comments = { "italic" },
        conditionals = { "italic" },
      },
      integrations = {
        cmp = true,
        gitsigns = true,
        nvimtree = true,
        treesitter = true,
        notify = true,
        mini = { enabled = true },
      },
    },
  },
{{- end }}

  -- Configure LazyVim to use the theme
  {
    "LazyVim/LazyVim",
    opts = {
      colorscheme = "{{ $themeData.tools.nvim_colorscheme }}",
    },
  },
}
